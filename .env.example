# Supabase insert-only analytics.
# Academic mapping:
# [CO] Client-side envs keep instrumentation setup visible to non-backend researchers.
# [DF] Missing values intentionally fall back to console logging instead of blocking the study.
# [ED] Console fallback keeps demo sessions smooth during classroom or pilot runs.
# [PR] Lightweight analytics helps compare receptivity across physical-digital touchpoints.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
